[SKILL: plan]

> **Brain root:** If env `QWEN_PROJECT_BRAIN_DIR` is set to a safe relative path, use that folder instead of `.project-brain` for every path below.

Read first (MANDATORY — in this order):

1. `docs/backend-tasks.md` — Generated by audit-backend skill (primary task source)
2. `docs/frontend-tasks.md` — Generated by audit-frontend skill (primary task source)
3. `PRD.md` or `docs/PRD.md` — Validate tasks cover all requirements
4. `ARCHITECTURE.md` or `docs/ARCHITECTURE.md` — Validate task order respects architecture
5. All audit files that exist:
   - `.project-brain/audit-backend.md`
   - `.project-brain/audit-frontend.md`
   - `.project-brain/audit-roles.md`
   - `.project-brain/audit-database.md`

**Your job:** Merge all task files into a single prioritized implementation plan. Do NOT create new tasks — only organize what the audit skills already identified.

---

## PHASE 1: MERGE AND DEDUPLICATE

Read `docs/backend-tasks.md` and `docs/frontend-tasks.md`:

- Extract all tasks marked Critical, High, Medium
- Combine into one list
- Remove duplicates (if same endpoint/feature appears in both)
- Preserve the original acceptance criteria

## PHASE 2: ADD DEPENDENCIES AND ORDER

For each task, determine:

- **Dependencies:** Does this task need another task first? (e.g., backend endpoint must exist before frontend can integrate)
- **Priority tier:** Keep the original Critical/High/Medium from the audit files
- **Estimated effort:** Low (<1hr) | Medium (few hrs) | Large (day+)

## PHASE 3: GENERATE IMPLEMENTATION PLAN

**Create or update: `docs/plan.md`** (or `PLAN.md` in project root)

### Format for `docs/plan.md`:

```markdown
# Implementation Plan

**Generated from:** backend-tasks.md, frontend-tasks.md, audit files
**Plan date:** <date>
**Total tasks:** <N>
**Estimated effort:** <total hours/days>

---

## Phase 1: Foundation (Critical - Must Complete First)

### Task 1: <name from task file>

- **Type:** Backend | Frontend | Database | Auth
- **Source:** backend-tasks.md Task N / frontend-tasks.md Task N
- **What:** <exactly what to build or fix>
- **Acceptance criteria:** <copy from original task file>
- **Files to create:** <list>
- **Files to modify:** <list>
- **Depends on:** none | Task N
- **Effort:** Low | Medium | Large

### Task 2: ...

---

## Phase 2: Core Features (Critical - After Foundation)

### Task N: ...

---

## Phase 3: User-Facing Features (High Priority)

### Task N: ...

---

## Phase 4: Polish and Edge Cases (Medium Priority)

### Task N: ...

---

## Implementation Order Summary

1. Task 1: <name> — <one-line why first>
2. Task 2: <name> — <one-line why second>
3. ...

**Dependencies graph:**
```

Task 1 → Task 3 → Task 5
Task 2 → Task 4 → Task 6
Task 7 (independent)

```

```

**Rules for plan generation:**

- Every task in the plan MUST come from an audit-generated task file
- NEVER invent new tasks — only organize what exists
- Preserve ALL acceptance criteria from the original task files
- Order MUST respect dependencies (backend before frontend integration, database before API, etc.)
- Mark tasks that can be parallel with ⚡ (same dependency level)

---

## PHASE 4: AUDIT REPORT

Write output to: `.project-brain/plan.md` using EXACTLY this format (all four sections, in this order — report is INVALID if any section is missing):

SUMMARY:
<one line — N tasks merged from audit files into implementation plan>
<one line — N critical, N high, N medium across X phases>
<one line — estimated total effort: <hours/days>> (3 lines max)

FINDINGS:

- backend-tasks.md: <N> tasks — <summary of coverage>
- frontend-tasks.md: <N> tasks — <summary of coverage>
- Combined plan: <N> unique tasks, <N> phases, <N> parallelizable
  (references only — never embed raw code blocks)

STATE:
<1–3 sentences: implementation order and key dependencies the build skill must respect, what can be done in parallel>

NEXT_SKILLS: build

Rules:

- Never embed raw code. Use file:line references only.
- ALWAYS generate the plan.md file — it is the primary output of this skill
- VERDICT: FAIL only for unrecoverable errors.

---

Append to: `.project-brain/work-log.md`
Add: `[<date>] skill:plan — <N> tasks merged from audit files into <M> phases`
